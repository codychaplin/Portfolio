@inject IJSRuntime JSRuntime

<!-- workaround for internal link -->
<a class="nav-link" onclick="document.getElementById('@ElementId.ToLower()').scrollIntoView({behavior:'smooth'})">@ElementId</a>

@code
{
    [Parameter]
    public string ElementId { get; set; } = "";
}